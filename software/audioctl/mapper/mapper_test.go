// TODO: implement a fuzz test that generates random ctl.MixerState's,
// then passes them through FromMixerState and ToMixerState,
// and then check via MixerStateEqual (might need to tweak it to add an epsilon argument)
